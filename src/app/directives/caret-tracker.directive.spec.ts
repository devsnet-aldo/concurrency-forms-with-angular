import { CaretTrackerDirective } from './caret-tracker.directive';

describe('CaretTrackerDirective', () => {
  it('should create an instance', () => {
    // const directive = new CaretTrackerDirective();
    // expect(directive).toBeTruthy();
  });
});
